# Template for /home/admin/accurate-giant/shared/.env on the production
# server (path still uses the legacy slug — see deploy/README.md "rebrand
# transition" note before renaming the directory).
#
# Copy to production.env, fill in real values, then scp it across
# (the real file is gitignored — never commit secrets here).
#
# Permissions on the server: 0600, owned by admin (the user supervisor
# runs the wrapper as).

NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
